# QA_App API Reference

## Functions

### `greet(name: str) -> str`
Returns a greeting string for the given name.

### `add(a: int, b: int) -> int`
Returns the sum of two integers.

### `slugify(text: str) -> str`
Converts a string to a URL-safe slug.

## Constants
- `APP_NAME` â€” application name
- `VERSION` â€” semantic version string
- `MAX_RETRIES` â€” default retry count
- `DEFAULT_TIMEOUT` â€” request timeout in seconds
